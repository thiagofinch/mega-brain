# Mind Map: ${PERSON}

> **Generated:** ${DATE}
> **Source DNA:** ${DNA_PATH}
> **Version:** ${VERSION}

---

## Summary

| Metric | Value |
|--------|-------|
| Total Nodes | ${TOTAL_NODES} |
| Total Edges | ${TOTAL_EDGES} |
| Avg Connections/Node | ${AVG_CONN} |
| Reasoning Chains | ${CHAINS} |
| Orphan Nodes | ${ORPHANS} |
| High-Confidence Edges (>0.8) | ${HIGH_CONF_PCT}% |

---

## Reasoning Chains (L5 -> L1)

### Chain ${N}: ${CHAIN_LABEL}

```
MET-${CODE}-${ID} (${met_label})
  ==> FW-${CODE}-${ID} (${fw_label})
    --> HEU-${CODE}-${ID} (${heu_label})
      --> MM-${CODE}-${ID} (${mm_label})
        --> PHI-${CODE}-${ID} (${phi_label})
```

---

## Clusters

### Cluster: ${CLUSTER_LABEL}

| Node | Layer | Strength | Connections |
|------|-------|----------|-------------|
| ${NODE_ID} | ${LAYER} | ${STRENGTH} | ${CONN_COUNT} |

---

## Cross-Person Connections

| Element A | Relation | Element B | Confidence | Note |
|-----------|----------|-----------|------------|------|
| ${ELEM_A} | ${TYPE} | ${ELEM_B} | ${CONF} | ${NOTE} |

---

## Orphan Nodes (Needs Review)

| Node | Layer | Possible Reason |
|------|-------|-----------------|
| ${NODE_ID} | ${LAYER} | ${REASON} |

---

## Edge Distribution

| Edge Type | Count | Avg Confidence |
|-----------|-------|----------------|
| SUPPORTS | ${N} | ${AVG} |
| IMPLEMENTS | ${N} | ${AVG} |
| EXTENDS | ${N} | ${AVG} |
| CONTRADICTS | ${N} | ${AVG} |
| REQUIRES | ${N} | ${AVG} |
| SPECIALIZES | ${N} | ${AVG} |

---

*Generated by Mind Mapper Workflow v1.0.0 -- Mega Brain*
